using UnityEngine;
using System.Collections;

public class CameraUpdate : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		/*
		Vector3 viewPosition = camera. WorldToViewportPoint(player.transform.position);
		
		if ( viewPosition.x > 0.9f )  // move right
			else if( viewPosition.x < 0.1f )
		// move left


		if ( viewPosition.y > 0.9f ) // move up
			else if( viewPosition.y < 0.1f ) // move down
		*/
}
}